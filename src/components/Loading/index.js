import React from "react";
import {LoadingContainer, LoadingStyled} from "./Loading.styles";

const Loading = () => {
    return (
        <LoadingContainer>
            <LoadingStyled/>
        </LoadingContainer>);
};

export default Loading;